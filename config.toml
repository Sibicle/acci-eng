# The URL the site will be built for
base_url = "https://accidental.engineering"

compile_sass = true
build_search_index = false

title = "accidental engineering"
description = "project build logs, fabrication tutorials, software development woes"
generate_feed = true

theme = "haxident"

taxonomies = [
    { name = "tags", rss = true },
]

[extra]
date_format="%e %B %Y"
menu_links = [
    { url = "$BASE_URL/tags/", name = "tags" },
    { url = "https://nedell.com", name = "portfolio" },
]

[markdown]
highlight_code = true
